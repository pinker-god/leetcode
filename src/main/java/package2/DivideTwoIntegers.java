package package2;

/**
 * @author pinker on 2018/1/24
 */
public class DivideTwoIntegers {
  public int divide(int dividend, int divisor) {
    if (dividend < divisor) {
      return 0;
    }
    int dividendLen = Integer.toString(dividend).length();
    int divisorLen = Integer.toString(divisor).length();
    return 0;
  }
}
